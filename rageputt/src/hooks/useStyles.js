const { makeStyles } = require("@material-ui/core");

const useStyles = makeStyles( {
    paper: {
        padding: 5,
        paddingLeft: 15,
        marginBottom: 20,
        borderRadius: 15
    },
    dialogi: {
        padding: 50,
    }
});
export default useStyles;